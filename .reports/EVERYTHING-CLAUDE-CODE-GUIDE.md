# Everything Claude Code - 신급 활용 가이드 (Enterprise Edition)

> **Everything Claude Code**를 기업 수준으로 활용하여 개발 생산성을 10배 향상시키는 완벽 가이드

## 📋 목차

1. [핵심 개념: Skills vs Agents vs Hooks vs Commands](#핵심-개념-skills-vs-agents-vs-hooks-vs-commands)
2. [Plan Mode vs Commands: 완벽 이해](#plan-mode-vs-commands-완벽-이해)
3. [Skills 완벽 가이드](#skills-완벽-가이드)
4. [Agents 완벽 가이드](#agents-완벽-가이드)
5. [Hooks 완벽 가이드](#hooks-완벽-가이드)
6. [Commands 완벽 가이드](#commands-완벽-가이드)
7. [🚀 Orchestrate: 자동화 파이프라인 (핵심!)](#orchestrate-자동화-파이프라인-핵심)
8. [실전 워크플로우](#실전-워크플로우)
9. [언어별 전문 워크플로우](#언어별-전문-워크플로우)
10. [기업급 Best Practices](#기업급-best-practices)

---

## 핵심 개념: Skills vs Agents vs Hooks vs Commands

### 🎯 한눈에 보는 차이점

| 구분 | 역할 | 실행 방식 | 사용 시점 | 예시 |
|------|------|----------|----------|------|
| **Skills** | 특정 작업 수행 | `/skill-name` 또는 Skill tool | 명시적 호출 | `/commit`, `/plan`, `/tdd` |
| **Agents** | 자율적 복잡 작업 수행 | Task tool로 호출 | Claude가 판단하여 호출 | `planner`, `code-reviewer` |
| **Hooks** | 이벤트 기반 자동화 | 자동 실행 (트리거 기반) | 특정 이벤트 발생 시 | 파일 저장 후 포맷팅 |
| **Commands** | 내장 CLI 명령 | `/command` (슬래시 명령) | 사용자 직접 호출 | `/help`, `/clear`, `/status` |

### 📊 계층 구조

```
┌─────────────────────────────────────────────────┐
│                   사용자 요청                    │
└─────────────────────────────────────────────────┘
                      ↓
        ┌─────────────┴─────────────┐
        ↓                           ↓
┌───────────────┐           ┌──────────────┐
│   Commands    │           │   Skills     │
│  (내장 CLI)   │           │ (커스텀 작업) │
└───────────────┘           └──────────────┘
                                    ↓
                            ┌──────────────┐
                            │   Agents     │
                            │ (자율 실행자) │
                            └──────────────┘
                                    ↓
                            ┌──────────────┐
                            │    Hooks     │
                            │ (자동 트리거) │
                            └──────────────┘
```

### 🔍 핵심 차이점 예시

**시나리오**: "새 기능 구현"

```bash
# 1️⃣ Command (내장 CLI)
/help                    # Claude Code 내장 명령어
/clear                   # 대화 내역 초기화
/status                  # 현재 상태 확인

# 2️⃣ Skill (커스텀 작업)
/plan                    # /plan 스킬 실행 (계획 수립)
/tdd                     # /tdd 스킬 실행 (TDD 워크플로우)
/commit                  # /commit 스킬 실행 (커밋 생성)

# 3️⃣ Agent (자율 실행)
"planner 에이전트로 계획 수립해줘"    # Task tool로 planner 에이전트 호출
"code-reviewer 실행해줘"            # Task tool로 code-reviewer 호출

# 4️⃣ Hook (자동 실행)
# Write tool 사용 → 자동으로 prettier 실행 (PostToolUse Hook)
# Session 종료 → 자동으로 instinct 저장 (PostSessionStop Hook)
```

---

## Plan Mode vs Commands: 완벽 이해

### 🤔 가장 헷갈리는 부분: Plan Mode란?

**Plan Mode**는 Claude Code의 **내부 상태**입니다. Skill이나 Agent가 아닙니다!

#### Plan Mode의 정체

```
┌──────────────────────────────────────────────────┐
│           Claude Code의 2가지 모드                │
├──────────────────────────────────────────────────┤
│                                                  │
│  1️⃣ Normal Mode (일반 모드)                      │
│     - 기본 상태                                   │
│     - 코드 작성, 파일 읽기, 명령 실행             │
│     - 즉시 실행 가능                              │
│                                                  │
│  2️⃣ Plan Mode (계획 모드)                        │
│     - EnterPlanMode tool로 진입                  │
│     - 코드 작성 불가 (읽기만 가능)                │
│     - 계획 문서 작성                              │
│     - ExitPlanMode로 탈출 (사용자 승인 필요)      │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 🆚 Plan Mode vs /plan Skill

| 구분 | Plan Mode | /plan Skill |
|------|-----------|-------------|
| **정체** | Claude Code 내부 상태 | Everything Claude Code 스킬 |
| **진입 방법** | `EnterPlanMode` tool | `/plan` 명령어 또는 Skill tool |
| **코드 작성** | ❌ 불가 (읽기만) | ✅ 가능 (planner agent 사용) |
| **목적** | 사용자 승인 필요한 계획 수립 | 복잡한 작업 계획 자동 생성 |
| **사용 빈도** | Claude가 자동 판단 | 명시적 호출 |
| **종료 방법** | `ExitPlanMode` tool | 자동 완료 |

### 📖 실제 워크플로우 비교

#### ✅ Plan Mode 워크플로우 (Claude Code 내장)

```
사용자: "전체 인증 시스템 리팩토링해줘"
         ↓
Claude: "이 작업은 복잡하므로 Plan Mode로 진입합니다"
         ↓
    [EnterPlanMode tool 호출]
         ↓
Claude: 코드베이스 탐색 (Read, Grep, Glob만 사용)
         ↓
Claude: 계획 문서 작성 (PLAN.md)
         ↓
    [ExitPlanMode tool 호출]
         ↓
사용자: 계획 승인/거부 (프롬프트 표시)
         ↓
사용자 승인 → Claude가 실제 코드 작성 시작
```

#### ✅ /plan Skill 워크플로우 (Everything Claude Code)

```
사용자: "/plan 사용자 인증 기능 추가"
         ↓
    [Skill tool 호출: "plan"]
         ↓
Claude: planner agent에게 위임
         ↓
Planner Agent: 
  - 코드베이스 분석
  - 위험 요소 식별
  - 단계별 계획 생성
  - 파일 목록 작성
         ↓
Agent 결과 반환 → 즉시 실행 가능한 계획 제공
```

### 🎯 언제 무엇을 사용해야 할까?

| 상황 | 사용할 것 | 이유 |
|------|----------|------|
| 대규모 리팩토링 (3개 이상 파일) | **EnterPlanMode** (자동) | 사용자 승인 필수 |
| 복잡한 기능 구현 계획 | **/plan skill** 명시적 호출 | 상세한 계획 + 즉시 실행 가능 |
| 간단한 버그 수정 | **둘 다 불필요** | 바로 코드 작성 |
| 아키텍처 변경 | **architect agent** | 기술적 결정 필요 |

### 💡 실전 예제

#### 예제 1: 간단한 기능 추가

```bash
# ❌ 잘못된 사용
사용자: "버튼 색상 변경해줘"
Claude: [EnterPlanMode 진입] # 불필요!

# ✅ 올바른 사용
사용자: "버튼 색상 변경해줘"
Claude: [즉시 코드 수정]
```

#### 예제 2: 복잡한 기능 구현

```bash
# ✅ 방법 1: Plan Mode (자동)
사용자: "전체 인증 시스템 구현해줘"
Claude: "이 작업은 복잡하므로 먼저 계획을 수립하겠습니다."
        [EnterPlanMode 자동 진입]
        → 계획 수립 → 사용자 승인 → 실행

# ✅ 방법 2: /plan Skill (명시적)
사용자: "/plan 전체 인증 시스템 구현"
Claude: [Skill tool 호출 → planner agent]
        → 상세 계획 반환 → 즉시 실행 가능
```

#### 예제 3: TDD 워크플로우

```bash
# ✅ /tdd Skill 사용
사용자: "/tdd 로그인 기능 구현"
Claude: [Skill tool 호출 → tdd-guide agent]
        1. 테스트 작성 (RED)
        2. 구현 (GREEN)
        3. 리팩토링 (REFACTOR)
        4. 커버리지 확인 (80%+)
```

---

## Skills 완벽 가이드

### 📚 전체 Skills 목록

#### 1️⃣ 계획 및 설계

| Skill | 명령어 | 목적 | 출력 |
|-------|--------|------|------|
| **plan** | `/plan` | 구현 계획 수립 | 단계별 계획, 위험 요소, 파일 목록 |

**사용 시점**:
- 3개 이상 파일 수정 예상
- 아키텍처 결정 필요
- 복잡한 기능 구현

**워크플로우**:
```bash
/plan [작업 내용]
  ↓
planner agent 호출
  ↓
- 요구사항 재정의
- 위험 요소 평가
- 단계별 계획 생성
- 파일 목록 작성
  ↓
계획 문서 반환 (즉시 실행 가능)
```

**예제**:
```bash
# 기본 사용
/plan 사용자 인증 시스템 구현

# 상세 지시
/plan OAuth 2.0 기반 소셜 로그인 구현
- Google, GitHub 지원
- JWT 토큰 기반
- Redis 세션 저장
```

#### 2️⃣ 테스트 주도 개발

| Skill | 명령어 | 목적 | 강제 사항 |
|-------|--------|------|----------|
| **tdd** | `/tdd` | TDD 방법론 강제 | 80%+ 커버리지 |
| **tdd-workflow** | `/tdd-workflow` | 전체 TDD 워크플로우 | Unit + Integration + E2E |

**사용 시점**:
- 새 기능 구현
- 버그 수정
- 리팩토링

**워크플로우**:
```bash
/tdd [기능 설명]
  ↓
tdd-guide agent 호출
  ↓
1. 🔴 RED: 실패하는 테스트 작성
  ↓
2. 🟢 GREEN: 최소 구현으로 통과
  ↓
3. 🔵 REFACTOR: 코드 개선
  ↓
4. 📊 COVERAGE: 80%+ 확인
  ↓
테스트 통과 + 고품질 코드
```

**예제**:
```bash
# 새 기능 구현
/tdd 사용자 프로필 업데이트 API 구현

# 버그 수정
/tdd "로그인 후 리다이렉트 안 되는 버그 수정"

# 리팩토링
/tdd 기존 auth 로직을 hook으로 리팩토링
```

#### 3️⃣ 코드 리뷰

| Skill | 명령어 | 목적 | 검사 항목 |
|-------|--------|------|----------|
| **code-review** | (자동) | 코드 품질 검토 | 품질, 패턴, 가독성 |
| **security-review** | `/security-review` | 보안 검토 | OWASP Top 10, 취약점 |
| **python-review** | (자동) | Python 전문 리뷰 | PEP 8, 타입 힌트 |
| **go-review** | `/go-review` | Go 전문 리뷰 | 관용구, 동시성 |

**Proactive 실행**:
```bash
# ✅ 자동 실행 (권장)
Claude: [코드 작성 완료]
        ↓
        [code-reviewer agent 자동 호출]
        ↓
        리뷰 결과 표시
        ↓
        CRITICAL/HIGH 이슈 자동 수정

# ❌ 수동 호출 (비권장)
"code-reviewer 실행해줘"  # Claude가 자동으로 해야 함
```

#### 4️⃣ 빌드 및 에러 해결

| Skill | 명령어 | 목적 | 언어 |
|-------|--------|------|------|
| (빌드 에러 해결) | (자동) | 빌드/타입 에러 수정 | TypeScript, JS |
| **go-build** | `/go-build` | Go 빌드 에러 수정 | Go |

**자동 트리거**:
```bash
npm run build  # 실패
  ↓
build-error-resolver agent 자동 호출
  ↓
1. 에러 메시지 분석
2. 최소 변경으로 수정
3. 빌드 재실행
4. 타입 에러 제거
  ↓
빌드 성공 ✅
```

#### 5️⃣ E2E 테스트

| Skill | 명령어 | 목적 | 프레임워크 |
|-------|--------|------|-----------|
| **e2e** | `/e2e` | E2E 테스트 생성/실행 | Playwright (Vercel Agent Browser 우선) |

**사용 시점**:
- Critical Flow 테스트 (로그인, 결제, 회원가입)
- 배포 전 회귀 테스트
- 주요 시나리오 검증

**워크플로우**:
```bash
/e2e [시나리오 설명]
  ↓
e2e-runner agent 호출
  ↓
1. Playwright 테스트 생성
2. Vercel Agent Browser로 실행 (우선)
3. 실패 시 Playwright 폴백
4. 스크린샷/동영상/trace 저장
5. Flaky 테스트 격리
  ↓
테스트 결과 + 아티팩트
```

**예제**:
```bash
# 기본 시나리오
/e2e 사용자 로그인 플로우 테스트

# 복잡한 시나리오
/e2e "결제 프로세스 테스트
1. 장바구니 추가
2. 배송지 입력
3. 결제 진행
4. 주문 완료 확인"
```

#### 6️⃣ 문서 및 유지보수

| Skill | 명령어 | 목적 | 출력 |
|-------|--------|------|------|
| (문서 업데이트) | (자동) | 문서 자동 업데이트 | README, CODEMAP |
| (리팩토링 정리) | (자동) | 데드 코드 제거 | knip, depcheck 보고서 |

#### 7️⃣ 학습 및 패턴

| Skill | 명령어 | 목적 |
|-------|--------|------|
| **continuous-learning** | (자동) | 세션에서 패턴 추출 |
| **continuous-learning-v2** | (자동) | Instinct 기반 학습 |
| **instinct-status** | `/instinct-status` | 학습된 Instinct 확인 |
| **instinct-export** | `/instinct-export` | Instinct 공유 |
| **instinct-import** | `/instinct-import` | Instinct 가져오기 |
| **skill-create** | `/skill-create` | Git 히스토리에서 스킬 생성 |

#### 8️⃣ 설정 및 도구

| Skill | 명령어 | 목적 |
|-------|--------|------|
| **configure-ecc** | `/configure-ecc` | Everything Claude Code 설치 |
| **keybindings-help** | (자동) | 키보드 단축키 커스터마이징 |

### 🎯 Skills 사용 전략

#### 전략 1: Always TDD

```bash
# ✅ 권장 워크플로우
1. /plan [기능 설명]          # 계획 수립
2. /tdd [기능 설명]            # TDD로 구현
3. (자동) code-reviewer       # 자동 리뷰
4. (자동) security-reviewer   # 자동 보안 검사
5. /e2e [시나리오]            # Critical Flow 테스트
```

#### 전략 2: Bug Fix Workflow

```bash
1. /tdd "버그 설명 및 재현 방법"
   - 버그 재현 테스트 작성
   - 테스트 실패 확인
   - 버그 수정
   - 테스트 통과 확인
2. /e2e 관련 시나리오 회귀 테스트
```

#### 전략 3: Refactoring Workflow

```bash
1. /plan 리팩토링 계획
2. (자동) refactor-cleaner   # 데드 코드 제거
3. /tdd 리팩토링 구현        # 테스트 유지
4. (자동) code-reviewer      # 품질 검증
```

---

## Agents 완벽 가이드

### 🤖 Agent vs Skill 차이점

```
┌──────────────────────────────────────────────┐
│                Skill (스킬)                   │
├──────────────────────────────────────────────┤
│ - 사용자가 명시적으로 호출                    │
│ - /skill-name 명령어                         │
│ - 즉시 실행                                   │
│                                              │
│ 예: /plan, /tdd, /e2e                        │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│               Agent (에이전트)                │
├──────────────────────────────────────────────┤
│ - Claude가 판단하여 자동 호출                 │
│ - Task tool 사용                             │
│ - 자율적 작업 수행                            │
│                                              │
│ 예: planner, code-reviewer, tdd-guide        │
└──────────────────────────────────────────────┘

💡 핵심: Skill은 "명령", Agent는 "자율 실행자"
```

### 📋 전체 Agents 목록

#### 🏗️ Planning & Architecture

##### 1. **planner** Agent

**목적**: 복잡한 기능 구현 전 상세 계획 수립

**자동 호출 조건**:
- 3개 이상 파일 수정 예상
- 아키텍처 결정 필요
- 사용자가 "계획", "plan" 키워드 사용

**워크플로우**:
```
Task tool 호출
  ↓
planner agent 시작
  ↓
1. 요구사항 재정의
2. 코드베이스 탐색 (Read, Grep, Glob)
3. 위험 요소 식별
4. 단계별 계획 생성
5. 파일 목록 작성
6. 기술적 트레이드오프 고려
  ↓
Agent 완료 → 결과 반환
  ↓
Claude가 사용자에게 요약 제시
```

**사용 예제**:
```bash
# Claude가 자동 판단
사용자: "전체 인증 시스템 구현해줘"
Claude: "이 작업은 복잡하므로 planner agent를 사용하겠습니다."
        [Task tool로 planner 호출]

# 명시적 요청
사용자: "planner 에이전트로 다크모드 구현 계획 수립해줘"
Claude: [Task tool로 planner 호출]
```

**출력 예제**:
```markdown
# Implementation Plan: User Authentication System

## Phase 1: Database Schema (2 files)
- `prisma/schema.prisma`: User, Session 모델
- `prisma/migrations/`: 마이그레이션

## Phase 2: Backend API (4 files)
- `src/api/auth/login.ts`: 로그인 엔드포인트
- `src/api/auth/logout.ts`: 로그아웃 엔드포인트
- `src/middleware/auth.ts`: 인증 미들웨어
- `src/utils/jwt.ts`: JWT 유틸리티

## Risks
⚠️  JWT 토큰 만료 처리
⚠️  CSRF 공격 방어
⚠️  비밀번호 해싱 강도

## Dependencies
- jsonwebtoken
- bcrypt
- express-session
```

##### 2. **architect** Agent

**목적**: 시스템 설계, 확장성, 기술적 결정

**자동 호출 조건**:
- 아키텍처 변경 요청
- 기술 스택 결정 필요
- 확장성/성능 논의

**워크플로우**:
```
Task tool 호출
  ↓
architect agent 시작
  ↓
1. 현재 아키텍처 분석
2. 요구사항 평가
3. 대안 비교 (장단점)
4. 확장성 고려
5. 성능 영향 분석
6. 기술 스택 추천
  ↓
아키텍처 문서 반환
```

**사용 예제**:
```bash
사용자: "architect 에이전트로 마이크로서비스 전환 계획 수립"
사용자: "WebSocket vs SSE vs Polling 중 어떤 걸 선택해야 할까?"
```

#### 🧪 Testing & Quality

##### 3. **tdd-guide** Agent

**목적**: TDD 방법론 강제, 80%+ 커버리지 보장

**자동 호출 조건**:
- `/tdd` skill 호출 시
- 새 기능 구현 시 (proactive)
- 버그 수정 시 (proactive)

**워크플로우**:
```
Task tool 호출
  ↓
tdd-guide agent 시작
  ↓
🔴 RED Phase
  - 실패하는 테스트 작성
  - 테스트 실행 → 실패 확인
  ↓
🟢 GREEN Phase
  - 최소 구현으로 통과
  - 테스트 실행 → 통과 확인
  ↓
🔵 REFACTOR Phase
  - 코드 개선 (중복 제거, 가독성)
  - 테스트 유지 (여전히 통과)
  ↓
📊 COVERAGE Phase
  - 커버리지 측정 (80%+ 필수)
  - 부족 시 테스트 추가
  ↓
Agent 완료 → 고품질 코드 + 테스트
```

**출력 예제**:
```typescript
// 🔴 RED: 테스트 작성
describe('User Profile Update', () => {
  it('should update user email', async () => {
    const result = await updateUserEmail('user123', 'new@example.com')
    expect(result.email).toBe('new@example.com')
  })
})

// 🟢 GREEN: 구현
async function updateUserEmail(userId: string, email: string) {
  const user = await db.user.update({
    where: { id: userId },
    data: { email }
  })
  return user
}

// 🔵 REFACTOR: 개선
async function updateUserEmail(userId: string, email: string) {
  validateEmail(email) // 유효성 검사 추가
  
  const user = await db.user.update({
    where: { id: userId },
    data: { email }
  })
  
  await sendEmailVerification(user) // 이메일 인증 추가
  return user
}

// 📊 COVERAGE: 90% (목표 달성 ✅)
```

##### 4. **code-reviewer** Agent

**목적**: 코드 품질, 보안, 유지보수성 검토

**자동 호출 조건** (MUST):
- 코드 작성/수정 완료 후 **즉시**
- 모든 코드 변경에 대해 **필수**

**워크플로우**:
```
Task tool 호출 (자동)
  ↓
code-reviewer agent 시작
  ↓
1. 코드 품질 분석
   - 가독성
   - 복잡도
   - 일관성
2. 보안 취약점 검사
   - XSS, SQL Injection
   - 인증/권한
3. 성능 이슈 탐지
   - N+1 쿼리
   - 불필요한 렌더링
4. Best Practices 준수
   - 언어별 관용구
   - 프레임워크 규칙
  ↓
이슈 등급 분류
  - CRITICAL: 즉시 수정 필수
  - HIGH: 수정 강력 권장
  - MEDIUM: 개선 권장
  - LOW: 선택적 개선
  ↓
Agent 완료 → 리뷰 결과 + 자동 수정 (CRITICAL)
```

**출력 예제**:
```markdown
# Code Review Results

## ❌ CRITICAL Issues (0)
None

## ⚠️  HIGH Issues (2)
1. **SQL Injection 위험** - `src/api/users.ts:42`
   - 문제: 사용자 입력을 직접 쿼리에 삽입
   - 수정: Parameterized query 사용
   ```typescript
   // ❌ Before
   db.query(`SELECT * FROM users WHERE name = '${userName}'`)
   
   // ✅ After
   db.query('SELECT * FROM users WHERE name = ?', [userName])
   ```

2. **비밀번호 평문 저장** - `src/auth/register.ts:15`
   - 문제: 비밀번호를 해싱하지 않고 저장
   - 수정: bcrypt로 해싱
   ```typescript
   // ❌ Before
   user.password = password
   
   // ✅ After
   user.password = await bcrypt.hash(password, 10)
   ```

## 💡 MEDIUM Issues (1)
1. **복잡한 중첩 로직** - `src/utils/formatter.ts:23`
   - 제안: 함수 분리로 가독성 향상

## ✅ LOW Issues (3)
...
```

##### 5. **python-reviewer** / **go-reviewer** Agent

**목적**: 언어별 전문 코드 리뷰

**차이점**:
- **python-reviewer**: PEP 8, 타입 힌트, Pythonic 관용구
- **go-reviewer**: Go 관용구, 동시성 패턴, 에러 처리

**자동 호출 조건**:
- Python/Go 파일 수정 시 자동

##### 6. **e2e-runner** Agent

**목적**: Playwright 기반 E2E 테스트 생성 및 실행

**자동 호출 조건**:
- `/e2e` skill 호출 시
- Critical Flow 변경 시 (proactive)

**워크플로우**:
```
Task tool 호출
  ↓
e2e-runner agent 시작
  ↓
1. 테스트 시나리오 분석
2. Playwright 테스트 생성
3. Vercel Agent Browser로 실행 (우선)
4. 실패 시 Playwright 폴백
5. 스크린샷/동영상/trace 캡처
6. Flaky 테스트 감지 및 격리
7. 테스트 결과 보고서 생성
  ↓
Agent 완료 → 테스트 결과 + 아티팩트
```

**출력 예제**:
```typescript
// tests/e2e/login.spec.ts
test('사용자 로그인 플로우', async ({ page }) => {
  await page.goto('https://example.com/login')
  
  await page.fill('[name="email"]', 'test@example.com')
  await page.fill('[name="password"]', 'password123')
  await page.click('button[type="submit"]')
  
  await expect(page).toHaveURL('/dashboard')
  await expect(page.locator('h1')).toContainText('Welcome')
})
```

**아티팩트**:
```
tests/e2e/
  artifacts/
    login-flow-success.png
    login-flow-success.webm
    login-flow-trace.zip
```

#### 🔧 Build & Fix

##### 7. **build-error-resolver** Agent

**목적**: TypeScript/빌드 에러 최소 변경으로 수정

**자동 호출 조건** (MUST):
- `npm run build` 실패 시
- `tsc` 에러 발생 시
- 타입 에러 발생 시

**워크플로우**:
```
빌드 실패 감지
  ↓
build-error-resolver agent 자동 호출
  ↓
1. 에러 메시지 분석
2. 최소 변경으로 수정
   - 타입 정의 수정
   - Import 경로 수정
   - 누락된 의존성 추가
3. 빌드 재실행
4. 성공할 때까지 반복
  ↓
Agent 완료 → 빌드 성공 ✅
```

**출력 예제**:
```bash
# 에러 분석
Error: Property 'email' does not exist on type 'User'
  at src/components/Profile.tsx:42

# 수정 적용
interface User {
  id: string
  name: string
  email: string  // ← 추가
}

# 빌드 재실행
✅ Build successful
```

##### 8. **go-build-resolver** Agent

**목적**: Go 빌드/vet/린터 에러 수정

**자동 호출 조건**:
- `go build` 실패 시
- `go vet` 경고 시

#### 🔒 Security

##### 9. **security-reviewer** Agent

**목적**: OWASP Top 10, 취약점 탐지 및 수정

**자동 호출 조건** (MUST):
- 인증/권한 코드 작성 후
- API 엔드포인트 생성 후
- 사용자 입력 처리 코드 작성 후
- 결제/민감 데이터 처리 코드 작성 후

**검사 항목**:
```
✅ OWASP Top 10
  - SQL Injection
  - XSS (Cross-Site Scripting)
  - CSRF (Cross-Site Request Forgery)
  - 인증/권한 우회
  - 민감 데이터 노출
  - XML 외부 엔티티 (XXE)
  - 안전하지 않은 역직렬화
  - 취약한 의존성
  - 부적절한 로깅/모니터링

✅ 추가 검사
  - 하드코딩된 시크릿
  - 약한 암호화
  - 안전하지 않은 CORS 설정
  - Rate Limiting 부재
```

**출력 예제**:
```markdown
# Security Review Results

## 🔴 CRITICAL Vulnerabilities (1)
1. **Hardcoded API Key** - `src/services/openai.ts:5`
   - Risk: API 키 노출 → 무단 사용 → 금전적 손실
   - Fix:
   ```typescript
   // ❌ Before
   const apiKey = 'sk-proj-xxxxxxxxxxxxx'
   
   // ✅ After
   const apiKey = process.env.OPENAI_API_KEY
   if (!apiKey) throw new Error('OPENAI_API_KEY not configured')
   ```

## ⚠️  HIGH Vulnerabilities (2)
1. **SQL Injection** - `src/api/search.ts:28`
2. **Missing Rate Limiting** - `src/api/auth/login.ts`

## ✅ Passed Checks (15)
- XSS Prevention: ✅
- CSRF Protection: ✅
- Secure Headers: ✅
...
```

##### 10. **database-reviewer** Agent

**목적**: PostgreSQL/Supabase 쿼리 최적화 및 보안

**자동 호출 조건**:
- SQL 쿼리 작성 후
- 데이터베이스 스키마 변경 후
- Prisma 모델 수정 후

**검사 항목**:
```
✅ 보안
  - SQL Injection 방지
  - RLS (Row Level Security) 정책
  - 권한 관리

✅ 성능
  - 인덱스 최적화
  - N+1 쿼리 방지
  - 쿼리 실행 계획 분석

✅ 스키마
  - 정규화 수준
  - 제약 조건 (FK, Unique)
  - 마이그레이션 안전성
```

#### 🧹 Maintenance

##### 11. **refactor-cleaner** Agent

**목적**: 데드 코드 제거, 중복 제거

**자동 호출 조건**:
- 리팩토링 요청 시
- 큰 변경 후 정리 필요 시

**워크플로우**:
```
Task tool 호출
  ↓
refactor-cleaner agent 시작
  ↓
1. 분석 도구 실행
   - knip: 미사용 export 탐지
   - depcheck: 불필요한 의존성
   - ts-prune: 미사용 TypeScript 코드
2. 안전한 제거 대상 식별
3. 영향 범위 분석
4. 순차적 제거
5. 빌드 확인
  ↓
Agent 완료 → 정리된 코드베이스
```

**출력 예제**:
```markdown
# Refactor Cleanup Report

## 제거된 파일 (5개)
- src/utils/old-formatter.ts (미사용)
- src/components/Legacy.tsx (미사용)
- src/hooks/useDeprecated.ts (미사용)

## 제거된 의존성 (3개)
- moment (대체: date-fns로 이미 전환 완료)
- lodash (대체: 네이티브 메서드 사용)
- request (대체: axios 사용)

## 제거된 코드 라인
- 총 1,247 라인 제거
- 번들 크기 감소: 45KB → 38KB (-15%)

## 빌드 상태
✅ Build: Passed
✅ Tests: Passed
✅ Type Check: Passed
```

##### 12. **doc-updater** Agent

**목적**: 문서 및 CODEMAP 자동 업데이트

**자동 호출 조건**:
- 큰 변경 후
- 배포 전
- 명시적 요청 시

**워크플로우**:
```
Task tool 호출
  ↓
doc-updater agent 시작
  ↓
1. /update-codemaps 실행
2. /update-docs 실행
3. CODEMAPS 생성/업데이트
   - docs/CODEMAPS/architecture.md
   - docs/CODEMAPS/api.md
4. README 업데이트
5. API 문서 갱신
  ↓
Agent 완료 → 최신 문서
```

---

## Hooks 완벽 가이드

### 🎣 Hook이란?

**Hook**은 특정 이벤트 발생 시 자동으로 실행되는 Shell 명령어입니다.

```
┌──────────────────────────────────────────┐
│         Hook 실행 흐름                    │
├──────────────────────────────────────────┤
│                                          │
│  이벤트 발생 (예: 파일 저장)              │
│          ↓                               │
│  Hook 트리거                              │
│          ↓                               │
│  Shell 명령어 실행                        │
│          ↓                               │
│  결과 피드백 (사용자에게 표시)            │
│                                          │
└──────────────────────────────────────────┘
```

### 📋 Hook 종류

#### 1️⃣ PostToolUse Hooks

**트리거**: 특정 Tool 사용 후

**설정 위치**: `~/.claude/settings.json`

**예제**:
```json
{
  "hooks": {
    "postToolUse": [
      {
        "tool": "Write",
        "filter": "*.ts",
        "command": "prettier --write {file_path}",
        "description": "TypeScript 파일 자동 포맷팅"
      },
      {
        "tool": "Edit",
        "filter": "*.go",
        "command": "gofmt -w {file_path} && goimports -w {file_path}",
        "description": "Go 파일 자동 포맷팅"
      },
      {
        "tool": "Write",
        "filter": "*.py",
        "command": "black {file_path} && isort {file_path}",
        "description": "Python 파일 자동 포맷팅"
      }
    ]
  }
}
```

**실행 흐름**:
```
Claude: [Write tool로 src/App.tsx 작성]
  ↓
PostToolUse Hook 트리거
  ↓
prettier --write src/App.tsx
  ↓
포맷팅 완료 메시지 표시
```

#### 2️⃣ PostSessionStop Hooks

**트리거**: 세션 종료 시

**사용 사례**:
- 학습된 패턴 저장
- 세션 요약 생성
- 임시 파일 정리

**예제**:
```json
{
  "hooks": {
    "postSessionStop": [
      {
        "command": "echo 'Session completed. Saving insights...'",
        "description": "세션 종료 로그"
      }
    ]
  }
}
```

#### 3️⃣ Custom Hooks (User-Defined)

**목적**: 사용자 정의 자동화

**예제**:
```json
{
  "hooks": {
    "postToolUse": [
      {
        "tool": "Edit",
        "filter": "*.tsx",
        "command": "eslint --fix {file_path}",
        "description": "ESLint 자동 수정"
      },
      {
        "tool": "Write",
        "filter": "package.json",
        "command": "npm install",
        "description": "의존성 자동 설치"
      }
    ]
  }
}
```

### 🎯 Hook 활용 전략

#### 전략 1: Formatting 자동화

```json
{
  "hooks": {
    "postToolUse": [
      {
        "tool": "Write",
        "filter": "*.{ts,tsx,js,jsx}",
        "command": "prettier --write {file_path}",
        "description": "JS/TS 자동 포맷팅"
      },
      {
        "tool": "Write",
        "filter": "*.{css,scss}",
        "command": "prettier --write {file_path}",
        "description": "CSS 자동 포맷팅"
      }
    ]
  }
}
```

#### 전략 2: Linting 자동화

```json
{
  "hooks": {
    "postToolUse": [
      {
        "tool": "Edit",
        "filter": "*.ts",
        "command": "eslint --fix {file_path}",
        "description": "ESLint 자동 수정"
      },
      {
        "tool": "Edit",
        "filter": "*.go",
        "command": "go vet {file_path}",
        "description": "Go vet 자동 실행"
      }
    ]
  }
}
```

#### 전략 3: Testing 자동화

```json
{
  "hooks": {
    "postToolUse": [
      {
        "tool": "Write",
        "filter": "*.test.ts",
        "command": "npm test -- {file_path}",
        "description": "테스트 자동 실행"
      }
    ]
  }
}
```

#### 전략 4: Build 검증

```json
{
  "hooks": {
    "postToolUse": [
      {
        "tool": "Edit",
        "filter": "*.ts",
        "command": "tsc --noEmit",
        "description": "타입 체크 자동 실행"
      }
    ]
  }
}
```

### ⚠️  Hook 사용 시 주의사항

```bash
# ❌ 나쁜 Hook: 느린 명령어
{
  "tool": "Write",
  "command": "npm run build",  # 매번 전체 빌드 (느림)
}

# ✅ 좋은 Hook: 빠른 명령어
{
  "tool": "Write",
  "command": "tsc --noEmit",  # 타입 체크만 (빠름)
}
```

**원칙**:
1. Hook 명령어는 **빠르게** (< 2초)
2. 실패해도 **안전하게** (워크플로우 중단 안 됨)
3. 명확한 **피드백** 제공

---

## Commands 완벽 가이드

### 🔧 Command란?

**Command**는 Claude Code의 **내장 CLI 명령어**입니다. Skill이나 Agent가 아닙니다!

```
┌──────────────────────────────────────────┐
│        Command vs Skill vs Agent         │
├──────────────────────────────────────────┤
│                                          │
│  Command (내장 CLI)                      │
│  - Claude Code 자체 기능                 │
│  - /help, /clear, /status               │
│  - Skill tool 사용 안 함                 │
│                                          │
│  Skill (커스텀 작업)                     │
│  - Everything Claude Code 플러그인       │
│  - /plan, /tdd, /e2e                    │
│  - Skill tool 사용                       │
│                                          │
│  Agent (자율 실행자)                     │
│  - Everything Claude Code 플러그인       │
│  - planner, code-reviewer               │
│  - Task tool 사용                        │
│                                          │
└──────────────────────────────────────────┘
```

### 📋 내장 Commands 목록

| Command | 목적 | 사용법 |
|---------|------|--------|
| `/help` | 도움말 표시 | `/help` |
| `/clear` | 대화 내역 초기화 | `/clear` |
| `/status` | 현재 상태 확인 | `/status` |
| `/tasks` | 실행 중인 Task 확인 | `/tasks` |
| `/settings` | 설정 확인 | `/settings` |

### 🔀 Command vs Skill 혼동 예제

```bash
# ❌ 잘못된 이해
사용자: "/commit은 command인가요?"
잘못된 답변: "네, command입니다."

# ✅ 올바른 이해
사용자: "/commit은 command인가요?"
올바른 답변: "아니요, /commit은 Skill입니다. Command는 /help, /clear 같은 Claude Code 내장 명령어입니다."

# 예제
/help     ← Command (Claude Code 내장)
/commit   ← Skill (Everything Claude Code 플러그인)
/plan     ← Skill (Everything Claude Code 플러그인)
/clear    ← Command (Claude Code 내장)
```

### 💡 Commands 사용 시나리오

#### 시나리오 1: 대화 초기화

```bash
# 새 작업 시작 전 대화 내역 정리
/clear

Claude: "대화 내역이 초기화되었습니다."
```

#### 시나리오 2: 도움말 확인

```bash
# Claude Code 사용법 확인
/help

Claude: "Claude Code 사용법:
- /help: 도움말 표시
- /clear: 대화 내역 초기화
- /status: 현재 상태 확인
..."
```

#### 시나리오 3: 실행 중인 Task 확인

```bash
# 백그라운드 작업 확인
/tasks

Claude: "실행 중인 Task:
1. task_abc123: planner agent (진행 중)
2. task_def456: e2e-runner agent (완료)
"
```

---

## 🚀 Orchestrate: 자동화 파이프라인 (핵심!)

### 💡 Orchestrate란?

**`/orchestrate`**는 **여러 에이전트를 자동으로 순차 실행**하는 메타 명령어입니다.

```
한 번의 명령 = 계획 → 구현 → 리뷰 → 보안 검사 → 최종 보고서
```

### 🎯 핵심 개념

```
┌──────────────────────────────────────────────┐
│         수동 (예전 방식)                      │
├──────────────────────────────────────────────┤
│                                              │
│  "planner로 계획해줘"                         │
│  [5분 대기...]                                │
│                                              │
│  "tdd-guide로 구현해줘"                       │
│  [10분 대기...]                               │
│                                              │
│  "code-reviewer로 리뷰해줘"                   │
│  [5분 대기...]                                │
│                                              │
│  총 소요: 20분 + 반복 설명                    │
│                                              │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│         자동 (Orchestrate)                    │
├──────────────────────────────────────────────┤
│                                              │
│  /orchestrate feature "인증 기능 구현"        │
│  [8분 자동 실행...]                           │
│                                              │
│  ✅ 모든 단계 자동 완료 + 최종 보고서         │
│                                              │
└──────────────────────────────────────────────┘
```

### 📋 사용 가능한 워크플로우

#### 1️⃣ Feature Workflow

```bash
/orchestrate feature "사용자 인증 기능 추가"
```

**자동 실행 순서:**
```
planner → tdd-guide → code-reviewer → security-reviewer
```

**각 단계:**
1. **planner**: 구현 계획 수립 (파일, 단계, 위험 요소)
2. **tdd-guide**: TDD로 구현 (테스트 → 구현 → 리팩토링)
3. **code-reviewer**: 코드 품질 검사 (자동 수정)
4. **security-reviewer**: 보안 취약점 검사 (자동 수정)

**최종 출력:**
```markdown
ORCHESTRATION REPORT
====================
Workflow: feature
Task: 사용자 인증 기능 추가
Duration: 8분 32초

SUMMARY
-------
JWT 기반 인증 완료. 
테스트 커버리지 92%, 보안 취약점 0개.

FILES CHANGED
-------------
- src/api/auth/login.ts (created)
- src/api/auth/logout.ts (created)
- src/middleware/auth.ts (created)

TEST RESULTS
------------
✅ 15/15 tests passing
✅ Coverage: 92%

SECURITY STATUS
---------------
✅ 0 CRITICAL, 0 HIGH vulnerabilities

RECOMMENDATION
--------------
🚀 SHIP - Ready for production
```

#### 2️⃣ Bugfix Workflow

```bash
/orchestrate bugfix "로그인 후 리다이렉트 안 되는 버그"
```

**자동 실행 순서:**
```
explorer → tdd-guide → code-reviewer
```

**각 단계:**
1. **explorer**: 버그 원인 조사
2. **tdd-guide**: 버그 재현 테스트 → 수정
3. **code-reviewer**: 수정사항 검토

#### 3️⃣ Refactor Workflow

```bash
/orchestrate refactor "클래스 컴포넌트를 함수형으로 변환"
```

**자동 실행 순서:**
```
architect → code-reviewer → tdd-guide
```

**각 단계:**
1. **architect**: 리팩토링 전략 수립
2. **code-reviewer**: 현재 코드 분석
3. **tdd-guide**: 테스트 유지하며 리팩토링

#### 4️⃣ Security Workflow

```bash
/orchestrate security "결제 모듈 보안 검토"
```

**자동 실행 순서:**
```
security-reviewer → code-reviewer → architect
```

**각 단계:**
1. **security-reviewer**: 보안 감사 (OWASP Top 10)
2. **code-reviewer**: 코드 품질 검사
3. **architect**: 아키텍처 개선 제안

#### 5️⃣ Custom Workflow

```bash
/orchestrate custom "architect,database-reviewer,tdd-guide,e2e-runner" "데이터베이스 최적화 및 캐싱 추가"
```

**원하는 순서로 에이전트 조합 가능!**

### 🔄 Handoff 시스템

각 에이전트는 다음 에이전트에게 **구조화된 문서**를 전달합니다:

```markdown
## HANDOFF: planner → tdd-guide

### Context
JWT 기반 인증 시스템 계획 수립 완료

### Findings
- 3개 API 엔드포인트 필요
- bcrypt로 비밀번호 해싱
- JWT 만료 시간: 1시간

### Files Modified
- src/api/auth/login.ts (new)
- src/api/auth/logout.ts (new)
- src/middleware/auth.ts (new)

### Open Questions
- Refresh token 구현 여부?
- Rate limiting 설정값?

### Recommendations
- 로그인 테스트부터 시작
- 80%+ 커버리지 목표
```

### 🎯 실전 예제

#### 예제 1: E-commerce 체크아웃

```bash
/orchestrate custom "planner,database-reviewer,tdd-guide,security-reviewer,e2e-runner" "결제 플로우 구현"
```

**실행 흐름:**
```
1. planner: 체크아웃 플로우 설계
2. database-reviewer: 주문/결제 테이블 최적화
3. tdd-guide: TDD로 구현
4. security-reviewer: 결제 보안 검사
5. e2e-runner: 전체 체크아웃 시나리오 테스트

최종: 프로덕션 준비 완료 보고서
```

#### 예제 2: 성능 최적화

```bash
/orchestrate custom "architect,refactor-cleaner,code-reviewer,build-error-resolver" "React 앱 성능 최적화"
```

**실행 흐름:**
```
1. architect: 병목 지점 식별
2. refactor-cleaner: 데드 코드 제거
3. code-reviewer: 최적화 검증
4. build-error-resolver: 빌드 확인

최종: 번들 크기 -30%, 렌더링 속도 +50%
```

### 💪 Orchestrate vs 수동 비교

| 항목 | 수동 방식 | Orchestrate |
|------|----------|-------------|
| 명령 횟수 | 4-5번 | 1번 |
| 소요 시간 | 25분 | 8분 |
| 컨텍스트 전달 | 수동 반복 설명 | 자동 handoff |
| 최종 보고서 | 없음 | 자동 생성 |
| 단계 누락 가능성 | 높음 | 0% |

### ⚡ 사용 팁

```bash
# ✅ 복잡한 작업에 사용
/orchestrate feature "실시간 채팅 구현"

# ✅ 보안 중요 작업
/orchestrate security "인증 모듈 검토"

# ❌ 간단한 작업에는 불필요
/orchestrate feature "README 오타 수정"  # 오버킬!
```

### 🎓 언제 사용해야 할까?

| 작업 복잡도 | Orchestrate 사용? |
|------------|------------------|
| 1-2 파일 수정 | ❌ 직접 처리 |
| 3-5 파일 수정 | ✅ 권장 |
| 6+ 파일 수정 | ✅✅ 필수 |
| 보안 중요 코드 | ✅✅ 필수 |
| 크로스 커팅 | ✅✅ 필수 |

---

## 실전 워크플로우

### 🚀 워크플로우 1: Feature 개발 (Full Stack)

```bash
┌─────────────────────────────────────────────────┐
│  Step 1: 계획 수립                               │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "/plan 사용자 프로필 편집 기능 구현"    │
│          ↓                                      │
│  [Skill tool → planner agent]                   │
│          ↓                                      │
│  출력: 단계별 계획 (5개 파일, 3개 Phase)         │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 2: TDD로 Backend 구현                      │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "/tdd 프로필 업데이트 API 구현"         │
│          ↓                                      │
│  [Skill tool → tdd-guide agent]                 │
│          ↓                                      │
│  1. 🔴 테스트 작성 (updateProfile.test.ts)      │
│  2. 🟢 API 구현 (updateProfile.ts)              │
│  3. 🔵 리팩토링 (중복 제거)                      │
│  4. 📊 커버리지: 92% ✅                          │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 3: 자동 보안 검토                          │
├─────────────────────────────────────────────────┤
│                                                 │
│  [security-reviewer agent 자동 실행]            │
│          ↓                                      │
│  검사: SQL Injection, XSS, 권한 검증             │
│          ↓                                      │
│  결과: ✅ Pass (0 vulnerabilities)              │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 4: TDD로 Frontend 구현                     │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "/tdd 프로필 편집 UI 컴포넌트 구현"     │
│          ↓                                      │
│  [Skill tool → tdd-guide agent]                 │
│          ↓                                      │
│  1. 🔴 테스트 작성 (ProfileEdit.test.tsx)       │
│  2. 🟢 컴포넌트 구현 (ProfileEdit.tsx)           │
│  3. 🔵 리팩토링 (커스텀 hook 분리)               │
│  4. 📊 커버리지: 88% ✅                          │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 5: 자동 코드 리뷰                          │
├─────────────────────────────────────────────────┤
│                                                 │
│  [code-reviewer agent 자동 실행]                │
│          ↓                                      │
│  검사: 품질, 성능, 가독성                        │
│          ↓                                      │
│  이슈: 1 MEDIUM (함수 분리 제안)                 │
│  자동 수정 적용 ✅                               │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 6: E2E 테스트                              │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "/e2e 프로필 편집 플로우 테스트"        │
│          ↓                                      │
│  [Skill tool → e2e-runner agent]                │
│          ↓                                      │
│  테스트 시나리오:                                │
│  1. 프로필 페이지 이동                           │
│  2. 이름 변경                                    │
│  3. 이메일 변경                                  │
│  4. 저장 버튼 클릭                               │
│  5. 성공 메시지 확인                             │
│          ↓                                      │
│  결과: ✅ All tests passed                      │
│  아티팩트: screenshot, video, trace             │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 7: 빌드 검증                               │
├─────────────────────────────────────────────────┤
│                                                 │
│  Claude: "npm run build 실행"                   │
│          ↓                                      │
│  빌드 실패 → [build-error-resolver 자동 실행]   │
│          ↓                                      │
│  타입 에러 수정 → 빌드 재실행 → ✅ Success       │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 8: 커밋 및 PR                              │
├─────────────────────────────────────────────────┤
│                                                 │
│  Claude: "변경사항 커밋 및 PR 생성"              │
│          ↓                                      │
│  git add . && git commit -m "feat: ..."        │
│  git push origin feature/profile-edit           │
│  gh pr create --title "..." --body "..."       │
│                                                 │
└─────────────────────────────────────────────────┘

✅ 완료: 고품질 기능 + 테스트 + 보안 + 문서
```

### 🐛 워크플로우 2: Bug Fix

```bash
┌─────────────────────────────────────────────────┐
│  Step 1: 버그 재현 테스트 작성                   │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "/tdd 로그인 후 리다이렉트 버그 수정"   │
│          ↓                                      │
│  [Skill tool → tdd-guide agent]                 │
│          ↓                                      │
│  1. 🔴 버그 재현 테스트 작성                     │
│     - 로그인 성공 후 /dashboard로 이동 확인      │
│     - 테스트 실행 → ❌ 실패 (버그 재현)          │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 2: 버그 수정                               │
├─────────────────────────────────────────────────┤
│                                                 │
│  [tdd-guide agent 계속]                         │
│          ↓                                      │
│  2. 🟢 버그 수정                                 │
│     - 리다이렉트 로직 수정                       │
│     - 테스트 실행 → ✅ 통과                      │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 3: 회귀 테스트                             │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "/e2e 로그인 플로우 전체 테스트"        │
│          ↓                                      │
│  [Skill tool → e2e-runner agent]                │
│          ↓                                      │
│  결과: ✅ All scenarios passed                  │
│                                                 │
└─────────────────────────────────────────────────┘

✅ 완료: 버그 수정 + 회귀 방지
```

### ♻️  워크플로우 3: 대규모 리팩토링

```bash
┌─────────────────────────────────────────────────┐
│  Step 1: 아키텍처 분석                           │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "architect 에이전트로 auth 모듈         │
│          리팩토링 계획 수립"                      │
│          ↓                                      │
│  [Task tool → architect agent]                  │
│          ↓                                      │
│  출력: 목표 아키텍처, 마이그레이션 경로          │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 2: 데드 코드 제거                          │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "refactor-cleaner로 미사용 코드 제거"   │
│          ↓                                      │
│  [Task tool → refactor-cleaner agent]           │
│          ↓                                      │
│  knip → 15개 미사용 export 제거                  │
│  depcheck → 3개 의존성 제거                      │
│  번들 크기: 45KB → 38KB (-15%)                   │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 3: 기존 동작 테스트 작성                   │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "/tdd 기존 auth 로직 테스트 작성"       │
│          ↓                                      │
│  [Skill tool → tdd-guide agent]                 │
│          ↓                                      │
│  현재 동작을 검증하는 테스트 스위트 생성          │
│  커버리지: 85% ✅                                │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 4: 점진적 리팩토링                         │
├─────────────────────────────────────────────────┤
│                                                 │
│  Phase 1: 인터페이스 분리                        │
│  Phase 2: 구현체 교체                            │
│  Phase 3: 레거시 코드 제거                       │
│          ↓                                      │
│  각 Phase마다:                                   │
│  - 코드 수정                                     │
│  - 테스트 실행 (여전히 통과 확인)                │
│  - code-reviewer 자동 실행                      │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 5: 최종 검증                               │
├─────────────────────────────────────────────────┤
│                                                 │
│  [병렬 실행]                                     │
│  1. code-reviewer                               │
│  2. security-reviewer                           │
│  3. build-error-resolver                        │
│  4. e2e-runner (Critical Flows)                 │
│          ↓                                      │
│  결과: ✅ All checks passed                     │
│                                                 │
└─────────────────────────────────────────────────┘

✅ 완료: 안전한 리팩토링 + 테스트 유지
```

### 🚢 워크플로우 4: 배포 전 체크리스트

```bash
┌─────────────────────────────────────────────────┐
│  배포 전 자동 검증 (병렬 실행)                   │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "5개 에이전트 병렬 실행하여 배포 준비   │
│          검증:                                   │
│          1. build-error-resolver - 빌드 검증    │
│          2. code-reviewer - 코드 품질           │
│          3. security-reviewer - 보안 검사       │
│          4. tdd-guide - 커버리지 확인           │
│          5. e2e-runner - Critical Flows"        │
│          ↓                                      │
│  [5개 Task tool 동시 호출]                       │
│          ↓                                      │
│  ┌────────────────────┐                         │
│  │ 1. Build: ✅       │                         │
│  │ 2. Code: ✅        │                         │
│  │ 3. Security: ✅    │                         │
│  │ 4. Coverage: 91% ✅│                         │
│  │ 5. E2E: ✅         │                         │
│  └────────────────────┘                         │
│          ↓                                      │
│  결과: 배포 준비 완료 🚀                         │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 🔐 워크플로우 5: 보안 강화 프로젝트

```bash
┌─────────────────────────────────────────────────┐
│  Step 1: 전체 보안 감사 (병렬)                   │
├─────────────────────────────────────────────────┤
│                                                 │
│  사용자: "4개 에이전트 병렬 실행:                │
│          1. security-reviewer - OWASP Top 10    │
│          2. database-reviewer - SQL 보안        │
│          3. code-reviewer - 민감 데이터 처리    │
│          4. go-reviewer - Go 보안 패턴"         │
│          ↓                                      │
│  [4개 Task tool 동시 호출]                       │
│          ↓                                      │
│  취약점 발견:                                    │
│  - 2 CRITICAL                                   │
│  - 5 HIGH                                       │
│  - 8 MEDIUM                                     │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 2: CRITICAL 이슈 즉시 수정                 │
├─────────────────────────────────────────────────┤
│                                                 │
│  [security-reviewer가 자동 수정 적용]           │
│          ↓                                      │
│  1. Hardcoded API Key → 환경 변수로 이동         │
│  2. SQL Injection → Parameterized query        │
│          ↓                                      │
│  재검증 → ✅ CRITICAL 이슈 0개                   │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 3: HIGH 이슈 수정                          │
├─────────────────────────────────────────────────┤
│                                                 │
│  [수동 수정 + 자동 검증]                         │
│          ↓                                      │
│  재검증 → ✅ HIGH 이슈 0개                       │
│                                                 │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│  Step 4: 최종 보안 감사                          │
├─────────────────────────────────────────────────┤
│                                                 │
│  [security-reviewer 재실행]                     │
│          ↓                                      │
│  결과:                                           │
│  - 0 CRITICAL ✅                                 │
│  - 0 HIGH ✅                                     │
│  - 3 MEDIUM (개선 권장)                          │
│  - 5 LOW (선택적)                                │
│                                                 │
└─────────────────────────────────────────────────┘

✅ 완료: 보안 수준 대폭 향상
```

---

## 언어별 전문 워크플로우

### 🐍 Python/Django 워크플로우

#### Skills 활용

| Skill | 사용법 | 목적 |
|-------|--------|------|
| `python-patterns` | 자동 적용 | Pythonic 관용구 |
| `python-testing` | `/tdd` 시 자동 | pytest 패턴 |
| `django-patterns` | Django 프로젝트 | DRF, ORM 베스트 프랙티스 |
| `django-security` | Django 인증/권한 | CSRF, SQL Injection 방지 |
| `django-tdd` | Django TDD | factory_boy, pytest-django |

#### 워크플로우 예제

```bash
# Django REST API 구현
/orchestrate custom "planner,django-patterns,django-tdd,django-security,python-reviewer" "사용자 API 구현"

# 실행 흐름:
1. planner: DRF ViewSet 구조 설계
2. django-patterns: Serializer, ViewSet 생성
3. django-tdd: pytest-django 테스트
4. django-security: RLS, permission 검증
5. python-reviewer: PEP 8, 타입 힌트 검사
```

### 🐹 Go 워크플로우

#### Skills 활용

| Skill | 사용법 | 목적 |
|-------|--------|------|
| `golang-patterns` | 자동 적용 | Go 관용구 (interfaces, errors) |
| `golang-testing` | `/go-test` | Table-driven tests |
| `go-build` | `/go-build` | 빌드 에러 해결 |
| `go-review` | `/go-review` | 동시성, 에러 처리 검토 |

#### 워크플로우 예제

```bash
# Go gRPC 서비스 구현
/orchestrate custom "planner,golang-patterns,go-test,go-build,go-review" "gRPC 서비스 구현"

# 실행 흐름:
1. planner: gRPC 서비스 구조 설계
2. golang-patterns: 인터페이스 정의, 에러 처리
3. go-test: Table-driven tests 작성
4. go-build: 빌드 에러 해결
5. go-review: 동시성 패턴, Race condition 검사
```

### ☕ Java/Spring Boot 워크플로우

#### Skills 활용

| Skill | 사용법 | 목적 |
|-------|--------|------|
| `springboot-patterns` | 자동 적용 | Layered Architecture |
| `springboot-security` | 보안 관련 | Spring Security 설정 |
| `springboot-tdd` | `/tdd` | JUnit 5, Mockito |
| `jpa-patterns` | DB 작업 | Entity, Repository 패턴 |

#### 워크플로우 예제

```bash
# Spring Boot REST API 구현
/orchestrate custom "planner,jpa-patterns,springboot-tdd,springboot-security" "주문 관리 API"

# 실행 흐름:
1. planner: Controller-Service-Repository 설계
2. jpa-patterns: Entity, Repository 생성
3. springboot-tdd: 단위/통합 테스트
4. springboot-security: JWT 인증, 권한 검증
```

### 🗄️ 데이터베이스별 워크플로우

#### PostgreSQL/Supabase

```bash
# Supabase 스키마 설계 및 RLS 설정
/orchestrate custom "database-reviewer,postgres-patterns,tdd-guide" "사용자 권한 시스템 구현"

# 실행 흐름:
1. database-reviewer: 스키마 분석, RLS 정책
2. postgres-patterns: 인덱스, 쿼리 최적화
3. tdd-guide: 데이터베이스 테스트
```

#### ClickHouse

```bash
# ClickHouse 분석 쿼리 최적화
/orchestrate custom "clickhouse-io,database-reviewer,code-reviewer" "분석 대시보드 쿼리 최적화"

# 실행 흐름:
1. clickhouse-io: 쿼리 패턴 분석
2. database-reviewer: 성능 최적화
3. code-reviewer: 코드 품질 검증
```

### 🎨 Frontend 전문 워크플로우

```bash
# React 컴포넌트 구현 (TypeScript)
/orchestrate custom "planner,frontend-patterns,tdd-guide,build-error-resolver,e2e-runner" "대시보드 컴포넌트 구현"

# 실행 흐름:
1. planner: 컴포넌트 구조 설계
2. frontend-patterns: React hooks, 상태 관리
3. tdd-guide: React Testing Library 테스트
4. build-error-resolver: TypeScript 에러 해결
5. e2e-runner: Playwright E2E 테스트
```

### 🔧 Backend 전문 워크플로우

```bash
# Node.js/Express API 구현
/orchestrate custom "planner,backend-patterns,database-reviewer,security-reviewer" "인증 API 구현"

# 실행 흐름:
1. planner: API 구조 설계
2. backend-patterns: Express 미들웨어, 에러 핸들링
3. database-reviewer: 쿼리 최적화
4. security-reviewer: JWT, Rate limiting
```

### 📊 언어별 선택 가이드

```bash
# TypeScript/JavaScript
/orchestrate feature + frontend-patterns + backend-patterns

# Python
/orchestrate feature + python-patterns + django-patterns

# Go
/orchestrate feature + golang-patterns + go-build

# Java
/orchestrate feature + springboot-patterns + jpa-patterns
```

---

## 기업급 Best Practices

### 🏆 골든 룰 (Golden Rules)

#### 1️⃣ Always Plan First

```bash
# ✅ DO: 복잡한 작업은 항상 계획 먼저
/plan [작업 내용]
  → 단계별 계획 확인
  → 위험 요소 파악
  → 실행

# ❌ DON'T: 계획 없이 바로 코드 작성
"인증 시스템 구현해줘"
  → 즉시 코드 작성 (위험!)
```

#### 2️⃣ Always TDD

```bash
# ✅ DO: 모든 구현에 TDD 적용
/tdd [기능 설명]
  → 테스트 먼저
  → 구현
  → 리팩토링
  → 80%+ 커버리지

# ❌ DON'T: 구현 후 테스트
코드 작성 → 나중에 테스트 (기술 부채!)
```

#### 3️⃣ Always Review

```bash
# ✅ DO: 코드 작성 직후 자동 리뷰
[코드 작성 완료]
  → [code-reviewer 자동 실행]
  → [security-reviewer 자동 실행] (민감 코드)
  → 이슈 수정
  → 재검증

# ❌ DON'T: 리뷰 없이 커밋
코드 작성 → 즉시 커밋 (품질 저하!)
```

#### 4️⃣ Always Parallel

```bash
# ✅ DO: 독립적 작업은 병렬 실행
"3개 에이전트 병렬 실행:
1. code-reviewer
2. security-reviewer
3. e2e-runner"

# ❌ DON'T: 순차 실행
code-reviewer → security-reviewer → e2e-runner
(3배 느림!)
```

#### 5️⃣ Always Secure

```bash
# ✅ DO: 민감 코드는 즉시 보안 검사
[인증 코드 작성]
  → [security-reviewer 자동 실행]
  → 취약점 수정
  → 재검증

# ❌ DON'T: 배포 전에만 보안 검사
코드 작성 → ... → 배포 직전 보안 검사
(너무 늦음!)
```

### 📊 지표 기반 품질 관리

#### 필수 지표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| **테스트 커버리지** | 80%+ | `/tdd` 자동 측정 |
| **빌드 성공률** | 100% | `build-error-resolver` |
| **보안 취약점** | 0 CRITICAL/HIGH | `security-reviewer` |
| **코드 품질** | 0 CRITICAL/HIGH | `code-reviewer` |
| **E2E 통과율** | 100% | `e2e-runner` |

#### 지표 대시보드 (예제)

```markdown
# 프로젝트 품질 대시보드

## 📊 현재 상태 (2026-02-06)

| 지표 | 현재 | 목표 | 상태 |
|------|------|------|------|
| 테스트 커버리지 | 92% | 80% | ✅ 초과 달성 |
| 빌드 성공률 | 100% | 100% | ✅ 목표 달성 |
| CRITICAL 취약점 | 0개 | 0개 | ✅ 목표 달성 |
| HIGH 취약점 | 0개 | 0개 | ✅ 목표 달성 |
| E2E 통과율 | 100% | 100% | ✅ 목표 달성 |

## 🎯 다음 목표
- 커버리지 95% 달성
- E2E 테스트 시나리오 +5개
```

### 🔄 CI/CD 통합

#### GitHub Actions 예제

```yaml
name: Everything Claude Code Validation

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      # 빌드 검증
      - name: Build Validation
        run: npm run build
        # build-error-resolver 로직과 동일
      
      # 테스트 커버리지
      - name: Test Coverage
        run: npm test -- --coverage
        # tdd-guide 기준: 80%+
      
      # 보안 검사
      - name: Security Audit
        run: npm audit --audit-level=high
        # security-reviewer 기준
      
      # E2E 테스트
      - name: E2E Tests
        run: npm run test:e2e
        # e2e-runner와 동일
      
      # 실패 시 알림
      - name: Notify on Failure
        if: failure()
        run: echo "Validation failed!"
```

### 🎓 팀 교육 자료

#### 신입 개발자 온보딩

```markdown
# Everything Claude Code 온보딩 가이드

## Day 1: 기본 개념
- [ ] Skill vs Agent vs Hook vs Command 이해
- [ ] Plan Mode vs /plan Skill 차이 이해
- [ ] 실습: /tdd로 간단한 기능 구현

## Day 2: TDD 워크플로우
- [ ] /tdd 스킬 마스터
- [ ] 80%+ 커버리지 달성 연습
- [ ] 실습: 버그 수정 with TDD

## Day 3: 보안 및 품질
- [ ] security-reviewer 사용법
- [ ] code-reviewer 자동 실행 설정
- [ ] 실습: 보안 취약점 수정

## Day 4: E2E 테스트
- [ ] /e2e 스킬 마스터
- [ ] Playwright 시나리오 작성
- [ ] 실습: 로그인 플로우 테스트

## Day 5: 종합 실습
- [ ] Feature 개발 Full Cycle
  - /plan → /tdd → review → /e2e → deploy
```

### 🏅 성숙도 모델

#### Level 1: 초급 (Basic)
- ✅ /tdd 사용
- ✅ code-reviewer 수동 실행
- ❌ 병렬 실행 미사용
- ❌ Hook 미설정

#### Level 2: 중급 (Intermediate)
- ✅ /plan + /tdd 조합
- ✅ code-reviewer 자동 실행
- ✅ security-reviewer 수동 실행
- ✅ Hook 기본 설정 (포맷팅)

#### Level 3: 고급 (Advanced)
- ✅ 전체 워크플로우 자동화
- ✅ 병렬 실행 활용
- ✅ 모든 리뷰어 자동 실행
- ✅ Hook 고급 설정 (린팅, 테스트)
- ✅ E2E 테스트 통합

#### Level 4: 신급 (Expert)
- ✅ 프로젝트별 커스텀 워크플로우
- ✅ CI/CD 완전 통합
- ✅ 지표 기반 품질 관리
- ✅ 팀 전체 표준화
- ✅ 지속적 개선 (Instinct Learning)

### 📈 ROI 측정

#### 투입 vs 산출

| 항목 | Before ECC | After ECC | 개선율 |
|------|------------|-----------|--------|
| **버그 수** | 평균 10개/릴리스 | 평균 2개/릴리스 | -80% |
| **배포 시간** | 4시간 | 2시간 | -50% |
| **테스트 커버리지** | 45% | 90% | +100% |
| **보안 취약점** | 평균 3개 CRITICAL | 0개 CRITICAL | -100% |
| **코드 리뷰 시간** | 2시간/PR | 30분/PR | -75% |

#### 절감 비용 계산

```
개발자 시급: $50/hr
팀 크기: 5명

월간 절감 시간:
- 자동 코드 리뷰: 5명 × 8hr/주 × 4주 = 160hr
- 자동 테스트 생성: 5명 × 4hr/주 × 4주 = 80hr
- 버그 수정 시간 감소: 5명 × 6hr/주 × 4주 = 120hr

총 절감 시간: 360hr/월
절감 비용: 360hr × $50 = $18,000/월
연간 절감: $216,000

투자 대비 수익률 (ROI): 무한대 (ECC는 무료)
```

---

## 🎯 결론 및 핵심 요약

### 핵심 4대 원칙

```
1️⃣ Plan → TDD → Review → Deploy
   모든 작업의 표준 워크플로우

2️⃣ Proactive > Reactive
   문제 발생 전 예방 (코드 작성 직후 리뷰)

3️⃣ Parallel > Sequential
   독립적 작업은 항상 병렬 실행

4️⃣ Automate Everything
   반복 작업은 Hook으로 자동화
```

### 즉시 적용 가능한 액션 아이템

#### 오늘 당장 (5분)

```bash
1. Hook 설정
   ~/.claude/settings.json에 포맷팅 Hook 추가

2. /plan + /tdd 조합 시도
   간단한 기능으로 연습
```

#### 이번 주 (1시간)

```bash
1. 전체 워크플로우 실습
   Feature 개발 Full Cycle 1회 완주

2. 팀원들과 공유
   Best Practices 문서화
```

#### 이번 달 (4시간)

```bash
1. CI/CD 통합
   GitHub Actions에 자동 검증 추가

2. 지표 대시보드 구축
   커버리지, 취약점 등 추적 시작

3. 팀 표준화
   모든 팀원이 Level 3 달성
```

### 최종 체크리스트

```markdown
## Everything Claude Code 마스터 체크리스트

### ✅ 기본 (Level 1)
- [ ] Skill vs Agent vs Hook vs Command 차이 이해
- [ ] Plan Mode vs /plan Skill 구분
- [ ] /tdd 스킬 사용 경험

### ✅ 중급 (Level 2)
- [ ] /plan + /tdd 조합 활용
- [ ] code-reviewer 자동 실행 설정
- [ ] Hook 기본 설정 (포맷팅)

### ✅ 고급 (Level 3)
- [ ] 전체 워크플로우 자동화
- [ ] 병렬 실행 활용
- [ ] E2E 테스트 통합
- [ ] 80%+ 커버리지 유지

### ✅ 신급 (Level 4)
- [ ] CI/CD 완전 통합
- [ ] 지표 기반 품질 관리
- [ ] 팀 전체 표준화
- [ ] ROI 측정 및 개선
```

### 참고 자료

```markdown
## 공식 문서
- Claude Code 공식 문서: https://docs.anthropic.com/claude-code
- Everything Claude Code GitHub: https://github.com/everything-claude-code

## 커뮤니티
- Discord: Claude Code Community
- GitHub Discussions: 질문 및 답변

## 추가 학습
- Skill 작성 가이드
- Agent 커스터마이징
- Hook 고급 패턴
```

---

---

## 🎓 고급 주제

### Instinct Learning System

**Continuous Learning V2**는 세션에서 자동으로 패턴을 학습합니다:

```bash
# Instinct 상태 확인
/instinct-status

# 출력:
Learned Instincts: 23
High Confidence (>0.8): 12
Medium Confidence (0.5-0.8): 8
Low Confidence (<0.5): 3

# Instinct 내보내기 (팀 공유)
/instinct-export team-instincts.json

# Instinct 가져오기
/instinct-import senior-dev-instincts.json
```

**Instinct → Skill 진화:**
```bash
/evolve

# 자동으로 자주 사용되는 Instinct를 Skill로 변환
```

### Skill Creator

```bash
# Git 히스토리에서 패턴 추출
/skill-create

# 분석 대상:
- 최근 50개 커밋
- 반복되는 패턴
- 코딩 스타일

# 출력: SKILL.md 파일 생성
```

### Agent 고급 기능

#### 백그라운드 실행

```bash
# 오래 걸리는 작업은 백그라운드로
"e2e-runner를 백그라운드로 실행해줘"

# Task ID 받음: task_abc123

# 나중에 결과 확인
/tasks
# 또는
"task_abc123 결과 확인해줘"
```

#### Model 선택

```bash
# 가벼운 작업: Haiku (빠르고 저렴)
"planner agent를 haiku로 실행"

# 복잡한 작업: Sonnet (균형)
# 기본값

# 매우 복잡한 작업: Opus (최고 성능)
"architect agent를 opus로 실행"
```

### Eval Harness

```bash
# 공식 평가 프레임워크
/eval-harness

# EDD (Eval-Driven Development)
1. 평가 기준 정의
2. 구현
3. 평가 실행
4. 개선
```

---

## 🚨 트러블슈팅

### 문제 1: Agent가 너무 느림

**해결:**
```bash
# Model을 Haiku로 변경
"code-reviewer를 haiku model로 실행"

# 또는 간단한 작업은 직접 처리
```

### 문제 2: Orchestrate가 중간에 실패

**해결:**
```bash
# 실패한 단계 확인
/tasks

# 해당 agent만 재실행
"tdd-guide agent 재실행"

# 또는 Custom workflow로 나머지만 실행
/orchestrate custom "code-reviewer,security-reviewer" "나머지 검증"
```

### 문제 3: Hook이 실행 안 됨

**해결:**
```bash
# ~/.claude/settings.json 확인
cat ~/.claude/settings.json

# Hook 문법 확인
# 올바른 filter: "*.ts"
# 틀린 filter: ".ts" (점만 있으면 안 됨)
```

### 문제 4: 테스트 커버리지가 80% 안 나옴

**해결:**
```bash
# tdd-guide에게 명시적 지시
/tdd "커버리지 85% 이상 달성할 때까지 테스트 추가"
```

---

## 📊 성능 최적화 팁

### 1. Model 선택 전략

```
Haiku: 경량 작업 (포맷 체크, 간단한 리뷰)
Sonnet: 일반 작업 (대부분)
Opus: 복잡한 작업 (아키텍처 결정)
```

### 2. 병렬 실행 극대화

```bash
# ✅ 병렬 가능
"3개 agent 병렬 실행: code-reviewer, security-reviewer, build-error-resolver"

# ❌ 병렬 불가능 (의존성 있음)
planner → tdd-guide (순차 필수)
```

### 3. 캐싱 활용

```bash
# 같은 파일을 여러 agent가 읽을 때 자동 캐싱
# 불필요한 재분석 방지
```

---

## 🎯 최종 요약

### Everything Claude Code의 핵심

```
1️⃣ /orchestrate = 자동화의 정점
   - 한 번의 명령으로 계획→구현→검증

2️⃣ Skills = 반복 작업 제거
   - /plan, /tdd, /e2e

3️⃣ Agents = 자율적 품질 관리
   - code-reviewer, security-reviewer (자동 실행)

4️⃣ Hooks = 완전 자동화
   - 파일 저장 → 자동 포맷팅/린팅

5️⃣ 언어별 전문화
   - Python, Go, Java, TypeScript 모두 지원
```

### 즉시 시작하기

```bash
# Step 1: 첫 번째 Orchestrate 실행
/orchestrate feature "간단한 기능 하나 구현"

# Step 2: Hook 설정
# ~/.claude/settings.json에 포맷팅 추가

# Step 3: 팀에 공유
# 이 가이드 + Instinct export
```

### 성공 공식

```
Orchestrate + TDD + Proactive Review + Hooks = 신급 개발자
```

---

**Remember**: Everything Claude Code는 도구입니다.  
올바른 워크플로우 + 명확한 지시 + 지속적 개선 = 10배 생산성 향상

**핵심 명령어 3개만 기억하세요:**
1. `/orchestrate feature "..."` - 새 기능
2. `/orchestrate bugfix "..."` - 버그 수정
3. `/orchestrate security "..."` - 보안 검사

**Start small, automate incrementally, measure everything.**

🚀 Happy Coding with Everything Claude Code!

---

## 📚 Quick Reference Card

```bash
# 필수 Workflows
/orchestrate feature "..."        # 새 기능
/orchestrate bugfix "..."         # 버그 수정
/orchestrate refactor "..."       # 리팩토링
/orchestrate security "..."       # 보안 검사

# 필수 Skills
/plan                            # 계획 수립
/tdd                             # TDD 구현
/e2e                             # E2E 테스트

# 유틸리티
/instinct-status                 # 학습 현황
/tasks                           # 실행 중인 작업
/help                            # 도움말

# Custom Orchestrate
/orchestrate custom "agent1,agent2,agent3" "설명"
```
